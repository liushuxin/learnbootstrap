import React from "react";
const DateFilter = ({ chartId }: any) => {
  return (
    <div
      style={{
        height: "300px",

        margin: "20px",
        background: "white"
      }}
    >
      日期组件{chartId}
    </div>
  );
};
export default DateFilter;
